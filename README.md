# SimMAP
Aplicação para contribuir no levantamento de dados de mapeamento de perfil.

Para rodar:

npm install<br>
npm run dev

Os dados de membros sao lidos e alterados diretamente em `db/db.json` pelas rotas `app/api/membros`, sem precisar subir `json-server`.
